{
  "compilerOptions": {
    "target": "esnext",                // Specifies the target ECMAScript version.
    "useDefineForClassFields": true,   // Use the new class field definitions.
    "lib": ["dom", "dom.iterable", "esnext"], // Include libraries for DOM, iterable, and ESNext features.
    "allowJs": true,                   // Allow JavaScript files to be compiled.
    "skipLibCheck": true,              // Skip type checking of declaration files.
    "strict": true,                    // Enable all strict type-checking options.
    "forceConsistentCasingInFileNames": true, // Enforce consistent casing in file names.
    "noEmit": true,                    // Do not output any files, only type-checking.
    "esModuleInterop": true,           // Enables emit interoperability between CommonJS and ES Modules.
    "module": "esnext",                // Specify module code generation method.
    "moduleResolution": "node",        // Module resolution strategy to use.
    "resolveJsonModule": true,         // Allows importing JSON modules.
    "isolatedModules": true,           // Ensures that each file can be safely transpiled independently.
    "jsx": "react-jsx"                 // Use the new JSX transform in React.
  },
  "include": ["src"],                  // Specify directories to include in the project.
  "exclude": ["node_modules", "dist"]  // Specify directories to exclude from the project.
}
